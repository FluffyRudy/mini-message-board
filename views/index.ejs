<head>
    <link rel="stylesheet" href="./styles.css">
</head>
<ul>
    <% messages.forEach((message) => { %>
        <li>
            <h1><%= message.user %></h1>
            <p><%= message.text %> </p>
            <p><%= message.added %></p>
        </li>
    <% }) %>
</ul>